This sealed recording is a synthetic compatibility fixture generated with the
unchanged `flight-fixed20-v1` algorithm at revision
`3ee5b7247f2e4525bea60cb8fa3e3e260b33db8c`. It uses fabricated neural response
metadata and invented descending controls refreshed every second. Generation
made zero model calls and captured no input. It is not an OBS06 model result.

The 600-tick trajectory starts at altitude 2 and ends at
`-27.022471970236644`, below the visible ground at -4. `fixture.json` records the
original algorithm hash and the hashes of the sealed manifest, events and
completion seal. Regression tests copy these files without changing their bytes
and restore private filesystem modes before reading them. Keep the fixture bytes
and recorded hashes unchanged when modifying current physics.
