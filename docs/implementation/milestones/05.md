# Milestone 05 — real-model feasibility and calibration decision

## Human-review revision — 2026-09-12 UTC

Human review returned **CHANGES_REQUESTED**. One bounded milestone-05 revision
is complete: **diagnostic_status: PASS · human_review: PENDING**. The current
interaction recommendation is **NO-GO for a playable core**; this supersedes the
historical proposed GO below. **Do not start milestone 06.**

Read the [complete historical trace diagnosis](../FEASIBILITY-05-DIAGNOSIS.md),
[comparison report](../FEASIBILITY-05-REVISION.md), and
[all scheduled results](../../../experiments/feasibility-revision-v2-results.json).
Every old episode was read and all 2,048 steps reconstructed before new neural
execution. V1 source/configurations/baseline and historical artifacts remain
unchanged. The separate `tangent_v2` module preserves legal tangential motion;
the default physics still uses v1 and its old no-sliding assertions.

The preregistered comparison used two fixed development seeds, both layouts,
black controls and 96-tick diagnostic cutoffs. Primary: 16 episodes / 1,536
calls / 580.6 seconds. Pad-cue loss on 334/384 candidate visual ticks met the
declared trigger for the single `overview16_v2` experiment: eight episodes /
768 calls / 315.6 seconds. All 24 executions completed, with no pad contacts;
their cutoffs are incomplete, not scored arena timeouts. The 2,304 calls and
896.1 measured phase seconds stayed within the declared 2,304-call / 1,350-second
budget. No further neural runs or condition tuning occurred.

Candidate visual stationary time fell from 197/384 ticks in v1 to 63/384,
while bottom-wall residence rose from 234 to 245 ticks. Full-scene sampling
retained cues on all 384 visual ticks, with 55 stationary and 286 bottom-wall
ticks. It supplies a static scene and therefore no avatar-position feedback.
Passive DNa02/DNa01/MDN/DNp09 rates show persistent downward requested drive.
All 768 corresponding v1 prefix steps match historical actions/state/input
hashes exactly; matching seed/window/pixel groups have identical rates/actions.
Task competence remains UNSUPPORTED; learning is NOT_RUN and disabled.

The revision began at `a1ecbdc44d9397c97a4af41f3f4abf84c1d050e0` with a clean
checkout. Evidence under
[`artifacts/milestones/05/revision/`](../../../artifacts/milestones/05/revision/)
contains preregistration, per-phase source/environment/baseline identity,
complete traces with exact input bytes and passive rates, actual trajectory/input
images, command argv/exits and test counts. The
[final identity](../../../artifacts/milestones/05/revision/final-source-identity.json)
binds the completed local revision; phase verifiers require unchanged execution
dependencies rather than relabeling historical evidence as current source.

| Revision boundary | Result and executed evidence |
|---|---|
| Historical artifacts | PASS: original verifier reconstructs 8 episodes / 2,048 actions and 14 paired probes; historical report digest unchanged. |
| Physics / crop / controller regressions | PASS: 330 focused tests; v1 assertions preserved, candidate walls/corners/obstacles/sweeps/replay and crop/telemetry checked. |
| New real artifacts | PASS: primary 16 / 1,536 steps and overview 8 / 768 steps independently verified, including exact trajectory/input image pixels and budget/trigger checks. |
| Required repository checks | PASS: `make verify`, 1,559 Python + 89 web-contract + 9 component + 2 fixture-browser tests; lint/types/build/locks/dependencies pass. No test failures/skips. |
| Real data integrity | PASS: fresh raw and graph checksum doctor. |
| Interaction suitability | NO-GO recommendation; no two-choice competence or engagement established, human review PENDING. |
| Full real release | BLOCKED: `make verify-real` exits 2 for unimplemented worker/end-to-end integration. |

Initial development collection/invocation failures are retained in the delegated
physics/diagnosis logs; fixes passed focused and full regression. Separate agents
owned physics, trace diagnosis and passive telemetry/verifier work. Root owned
the preregistration, neural runs, integration, reports and visual review.
No milestone 06 work or external publication occurred. The next handoff is
**human review of this milestone-05 revision**.

## Historical v1 milestone record

**automated_status: PASS · human_review: PENDING · unfinished pending review**

Executed [prompt 05](../../../flytrap-kit/prompts/05-feasibility.md) through
[resume](../../../flytrap-kit/prompts/resume.md) on 2026-09-12 UTC
(2026-09-11 US/Central). Only 05 was implemented. Automated feasibility work is
complete; the human behavior/interaction review is required before 06.
Learning evidence is **NOT_RUN** and learning remains disabled.

## Prerequisites and preservation

The checkout started clean at `e999df631277d94277747271bc0a8b1359682b29`, which
committed the previously recorded work. Every file hash in 04's final identity
still matched. Its report's older HEAD is historical, not the current checkout
identity. Raw/graph hashes passed a fresh `make data-doctor`. See
[prerequisite-check.json](../../../artifacts/milestones/05/prerequisite-check.json).
The unchanged graph, model, arena, renderer, runtime and locks permit reuse of
03–04's earlier boundary evidence; full-model execution was also performed here.

Preserved upstream files, LICENSE, NOTICE, attribution, kit and prior application
code. Preexisting file changes are limited to Makefile targets and STATUS. New
files are development runner/analysis/verifier scripts, 77 analysis/verifier tests,
experiment configurations/results, and this report plus FEASIBILITY documentation.
The changes are local and uncommitted; no external write occurred.

One bounded agent implemented the analysis helpers/tests, independent artifact
verifier/tests, and a read-only acceptance review. Root owned the experiment,
real execution, integration, result decisions, visual inspection and handoff.

## Measured result and decision

Read [FEASIBILITY](../FEASIBILITY.md) for the complete eight-trial table, method,
measured costs, limitations and proposed budgets. The compact machine report is
[feasibility-v1-results.json](../../../experiments/feasibility-v1-results.json).

The full graph has 165,122 neurons and 10,228,000 fast edges, graph SHA256
`4279e2222475b986a096634ea5f6ac9518d0f8feff3f3c2ed52ad1139d3cd2ed`.
All-one gains, existing visual calibration and `windowed_reset` remain explicit.
Two fixed development seeds were tested against seven pixel inputs with two
reset repetitions: 28 real neural windows. All 14 pairs repeated with identical
canonical serialized outputs. Motor outputs and spike telemetry changed for both
seeds under all six non-dark inputs versus dark; swapping canonical layouts also
changed motor outputs for both seeds. This establishes local visual sensitivity,
not task recognition or learning.

All eight full arena episodes reached **trial_timeout at 256 ticks**. There were
**zero model execution failures and zero successes**. The recorded paths remain
near the lower boundary; 43.4–57.8% of ticks were stationary despite continued
neural/motor activity. Both layouts, dark controls and a declared 0.5 motor scaling
variant are retained. No trajectory was corrected or success manufactured.

There were **2,076 actual model calls**, including **2,048 episode steps**.
Baseline preparation took 1.926 seconds and controller construction 3.200 seconds;
combined startup was 5.125 seconds. Construction used a fresh process after
cache-warming preparation; OS caches were not evicted. Step min/median/p95/max
were 0.289/0.399/0.447/0.558 seconds. Episodes took 101.04–103.47 seconds.
Peak process RSS was 739.1 MiB. Each episode represents 5,120 neural milliseconds,
not 100 seconds of simulated neural time. Hardware/software identities and hashes
are recorded; this workstation was not isolated from other local load.

**GO for a queued experimental core with explicitly labeled replay, subject to
human review.** Task-solving competence is not demonstrated, and a real-time or
learning-improvement claim is unsupported. The plan explicitly does not require
success on an arbitrary task to establish a functional pipeline. The review must
judge whether these honest unsuccessful runs make a worthwhile interaction.

[core-v1.json](../../../experiments/core-v1.json) freezes the selected condition
before any held-out evaluation: existing zero-distraction two-choice preset,
stripes reward mapping, both layouts, 8-pixel movement, multiplier 1.0, 256 ticks,
all-one gains and windowed resets. The half-speed development control did not
provide evidence warranting a new action mapping. Proposed limits derived from
cost are one active and one queued job, a 240-second resource deadline, 1,536 MiB
simulator memory and one-second progress snapshots. These remain proposals for
later worker/container enforcement and measurement, not implemented admission.

## Executed validation

[commands.jsonl](../../../artifacts/milestones/05/commands.jsonl) records root
validation argv, UTC starts, durations, exits and logs. Paths below are relative
to `artifacts/milestones/05/`.

| Command / boundary | Actual result | Evidence |
|---|---|---|
| `make data-doctor` | Exit 0; pinned raw and graph checksums PASS | `data-doctor.*.log` |
| `.venv/bin/python -m scripts.feasibility --output artifacts/milestones/05/trials` | Exit 0; 8/8 complete trials, 28/28 probe calls; 833.50 s total; no execution failures | `development.*.log`, `trials/report.json`, all per-trial traces |
| `.venv/bin/python -m pytest tests/unit/test_feasibility_analysis.py --junitxml=artifacts/milestones/05/analysis-unit.xml` | Exit 0; 36 passed, 0 failed/errors/skipped | `analysis-unit.*.log`, `analysis-unit.xml` |
| `make verify EVIDENCE=artifacts/milestones/05` | Exit 0; Python/web/browser/types/build/dependencies PASS | `verify.*.log` |
| Python within verify | 1,405 collected/passed; 0 failed/errors/skipped | `python.xml`, coverage files |
| Web contracts / components / browser startup | 89 / 9 / 2 passed; 0 failures/skips/flaky retries | `web-contract.xml`, `ui.xml`, `browser/` |
| `.venv/bin/python -m pytest tests/unit/test_feasibility_verifier.py --junitxml=artifacts/milestones/05/verifier-final.xml` | Exit 0; final 41 passed; 0 failed/errors/skipped | `verifier-final.*.log`, `verifier-final.xml` |
| `make lint` final | Exit 0; final verifier/test edit lint PASS | `lint-final.*.log` |
| `make verify-feasibility EVIDENCE=artifacts/milestones/05` final | Exit 0; all 2,048 actions/states/hashes replayed; 14 probes/28 repetitions checked | `feasibility-final.*.log`, `verification.json` |
| `env PYTHONPATH=. .venv/bin/python artifacts/milestones/05/build_report.py` | Exit 0; verified report, configuration and image contact sheets generated | `report.*.log`, `build_report.py` |
| Runner with `--graph-root artifacts/milestones/05/absent-graph` and separate output | Exit 1; diagnostic FAIL retained; zero model calls, no fixture fallback | `missing-data.*.log`, `missing-data/report.json` |
| `make verify-real` | Make exit 2 / CLI exit 1; full release gate BLOCKED on later integration | `verify-real.*.log` |

The final verifier change requires exact canonical serialization equality, including
signed zero. It and one added test landed after the full suite had collected;
the affected 41 tests, lint and full artifact verification were then rerun.
The ordinary suite's other code is unchanged. Do not add overlapping focused
runs to the suite count. All 77 new cases have passing evidence; the full suite
collected 1,405 before the last added case, not 1,406.

An agent's initial verifier test run had 29 passes and one failure in its synthetic
fixture setup: changing the observation condition also required recomputing that
fixture's summary. The correction preceded 40 passing tests; the signed-zero
addition then produced the final 41. Tests were not weakened. Exact commands,
exits and available excerpts are in `agent-validation.json`; original agent raw
logs and initial overwritten JUnit were not retained, which that record discloses.
Root final logs/JUnit supply the gate evidence. The evidence finalizer first
failed because it checked a link to its own source-identity output before creating
that file; moving link validation after output creation repaired the ordering.
The initial exit-1 log is retained. Existing AnyIO deprecation and
Node color-environment warnings remain; dependency audit found zero vulnerabilities.

The verifier checks physics, sampling, declarations and artifact consistency; it
does not independently reproduce the neural engine. Tests reject incomplete
schedules, corrupted traces/hashes/physics, wrong input/scaling, false fixture
labels, inconsistent telemetry, nonfinite metrics and false repeatability.
The real command separately executes the actual model. No fixture validates its
real-data gate. Prior 03 upstream regressions are reused evidence, not rerun here.

## Source identity and inspected evidence

The real run captured its execution tree and environment before later verifier,
Makefile and report edits. Its report SHA256 is
`6f37c02bf4c1160a0758de9b43ff2a41cf27872ecd3536265cfd95f7b5f93c85`.
The verifier requires current execution dependencies to match that snapshot.
[final-source-identity.json](../../../artifacts/milestones/05/final-source-identity.json)
records the final handoff source, including later validation/reporting files;
`summary.json` and `test-counts.json` bind final evidence and review state.

Root opened the trajectory contact sheet containing all eight overlays, the
seven-input contact sheet, one full-size trajectory, and both browser captures.
All trajectories start at the lower center and stay near the lower wall; none
reaches a pad. Swapped textures, blank-control labeling, half-speed labels and
actual timeout/neural-time disclosures are visible. Probe images contain only
intended grayscale pixels. The browser still shows the fixture foundation with
closed admission and learning not evaluated; its desktop/mobile layouts fit.
Both traces contain HTTP 200 JSON config responses plus an aborted duplicate
request. These browser checks validate foundation startup, not a full model UI.
`visual-review.json` records file hashes and findings. Agent inspection is not
human-gate approval.

## Human review and handoff

**human_review: PENDING.** Review:

1. [All eight trajectories](../../../artifacts/milestones/05/trajectories.png),
   [input pixels](../../../artifacts/milestones/05/probe-inputs.png), and the
   complete trial/cost table in [FEASIBILITY](../FEASIBILITY.md).
2. The queued-computation/labeled-replay mode and roughly 102-second episode cost.
3. The frozen zero-distraction preset and lack of demonstrated task-solving or
   learning benefit. Decide whether to approve this interaction or request changes.

For a new run, use `make feasibility EVIDENCE=artifacts/milestones/05-review` after
`make data-doctor`; then `make verify-feasibility EVIDENCE=artifacts/milestones/05-review`.
The chosen output root must be new. Existing evidence is available without running
any service. No full-model browser URL exists at this milestone.

[resume.md](../../../flytrap-kit/prompts/resume.md) explicitly requires human
review after 05 and says: **“Stop before starting the next milestone.”** An identical
resume instruction is not approval. Record approval against this report and frozen
configuration; substantive behavior changes require a new review.

| Boundary | Status | Handoff |
|---|---|---|
| Prerequisites / raw graph / preservation | PASS | Runtime/data/model/arena identities retained. |
| Full graph → pixels-only controller → arena | PASS | Finite measured real output, complete traces and scored timeouts. |
| Visual sensitivity / repeatability | PASS | Matched first-window probes; development evidence only. |
| Analysis / replay integrity / regression suite | PASS | Negative tests and actual artifact reconstruction. |
| Playable interaction decision | PENDING human review | Proposed GO for queued experimental core. |
| Task-solving / learning benefit | NOT_RUN learning; competence not demonstrated | Claims disabled; no held-out evaluation. |
| Full real-model release | BLOCKED | Durable queue/worker/API/replay/browser integration remains later work. |

Next: **human review of 05**, then **06 — database and artifact repositories**
only after approval. No later milestone, hosted CI, deployment, push, PR, paid
service, token transaction or social post was executed.
