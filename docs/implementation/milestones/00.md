# Milestone 00 — repository audit

**Audit deliverables: PASS. Baseline suite: FAIL. Real-model readiness: BLOCKED.**
Executed only [prompt 00](../../../flytrap-kit/prompts/00-audit.md). No application
features or regression fixes were implemented. Next milestone remains unstarted.

## Identity and preserved work

Actual HEAD and kit reference both equal
`8748e5bd30794d14afeb3441904221b52a002cac`; there is no revision delta.
Initial `git status --short` returned only `?? flytrap-kit/`. All 24 files listed
in the kit checksum manifest matched; the final preservation check also covers the
manifest itself. Root/ancestor instruction discovery found no existing AGENTS.md;
the user-supplied guidance was carried into the new root file.

Reference tracked-tree SHA256:
`5745aeef33457f4cb867954e902c03705d32bfda2d713ec3379435a9892207a7`.
It hashes compact sorted JSON mapping tracked paths to SHA256 of HEAD file bytes;
the exact definition and per-file hashes are in `source-identity.json`.
Final output hashes are in `final-source-identity.json`.

Changes are root AGENTS.md, five implementation documents, and a scoped
`/artifacts/` ignore entry. No upstream program/test/notice/asset or kit file was
edited. Evidence helpers and outputs are local ignored files. No commit was made.

## Findings and decisions

Verified against actual code: `W[post,pre]`; discarded zero-fast-weight modulatory
edges; incorrect reverse-direction mushroom lookup; per-call RNG/membrane/refractory
reset; CWD-relative annotation reads; implicit checkpoint loading and swallowed
errors. [SOURCE_MAP](../SOURCE_MAP.md) supplies source lines, dependencies,
entrypoints, and follow-up obligations. Parent inspected the relevant code and
reconciled a read-only source-audit agent's findings.

Preserve `windowed_reset`, inject annotation paths later, retain anatomy separately,
and require explicit gains/checkpoints. CHOSEN and reported learning/performance
numbers are upstream assertions, not reproduced results. Learning claim status is
**NOT_RUN**. Future controls are documented in [ARCHITECTURE](../ARCHITECTURE.md) and
[THREAT_MODEL](../THREAT_MODEL.md); none is claimed implemented by documentation.

## Actual validation

All commands ran from `/home/kernel_sanders/dev/flycoinrh`. Recorded commands below
were wrapped as `python artifacts/milestones/00/capture.py NAME COMMAND...`; the
wrapper saved exact argv, UTC start, duration, exit, stdout, and stderr in
[`commands.jsonl`](../../../artifacts/milestones/00/commands.jsonl).

| Command | Exit / result | Evidence under `artifacts/milestones/00/` |
|---|---|---|
| `python -m unittest discover -s . -p 'test_*.py' -v` | 1 / FAIL: 121 passed, 0 assertion failures, 1 import error, 0 skips | `baseline-unittest.stdout.log`, `baseline-unittest.stderr.log`, `baseline-summary.json` |
| `python artifacts/milestones/00/environment_probe.py` | 0 / environment captured; corrected repeat records exact raw-data filenames | `environment-corrected.stdout.log` (authoritative), original `environment.stdout.log` retained |
| `git show -s --format=fuller HEAD` | 0 / source revision | `git-identity.stdout.log` |
| `git remote -v` | 0 / local configured remotes | `git-remotes.stdout.log` |
| `git diff 8748e5bd30794d14afeb3441904221b52a002cac -- . ':!.gitignore'` | 0 / empty tracked source diff | `reference-diff.stdout.log` |
| `python artifacts/milestones/00/inventory.py` | 0 / 24 kit checksums matched; tracked change only `.gitignore`; baseline summary parsed | `inventory.stdout.log`, `source-identity.json` |
| `node --version`, `npm --version`, `uv --version`, `docker --version` | Each 0 / 26.8.2, 12.0.2, 0.10.9, 29.7.2 | Corresponding `*-version.stdout.log` |
| `/home/kernel_sanders/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome --version` | 0 / 151.0.7922.34 | `browser-version.stdout.log` |
| `python artifacts/milestones/00/final_check.py` | 0 / PASS: 56 upstream files and all 26 kit files preserved; six audit documents and 20 local links checked; repeated after final report update | `final-check.stdout.log`, `final-check-reviewed.stdout.log`, `final-source-identity.json` |
| `git diff --check` | 0 / PASS: tracked whitespace validation | `diff-check.stdout.log`, `diff-check.stderr.log` |
| `git status --short` | 0 / only `.gitignore`, new AGENTS/docs, and preexisting kit listed | `final-status.stdout.log` |

Unittest reports “Ran 122 tests” because it counts a `_FailedTest` placeholder for
the failed `test_pons` import. Only 121 actual test methods ran. Static inventory
finds 29 more in that module, not counted as passes or skips. `eth_abi` is absent.
No dependency was installed and no test was modified to hide the failure. The
printed “posted” messages came from mocked publication tests; no social post was
sent. Raw unittest output and JSON counts are retained; JUnit, coverage, browser
screenshots/traces, and real-model output were not generated.

Initial read-only inspection additionally used `pwd`, `ls -la`, `rg --files`
(including hidden instruction/config discovery), parent AGENTS existence checks,
`git status --short`, `git log -5 --oneline`, `git rev-parse HEAD`, `git diff --stat`,
`git ls-files`, `command -v`, `cat`/`sed` source and instruction reads, and `rg -n`
source/test/entrypoint searches. These early exploratory reads predated the durable
recorder; their outputs were inspected in the tool transcript. Persisted identity,
inventory, and environment commands above provide the reproducible audit facts.

## Boundary status and next handoff

| Boundary | Status | Remaining prerequisite |
|---|---|---|
| Audit/source preservation/working agreement | PASS | Retain ignored evidence with the checkout. |
| Upstream offline tests | FAIL | Resolve missing eth-abi/eth-utils in an isolated upstream-test profile before claiming full baseline pass. |
| Python-linked SQLite version | PASS | Lock validated runtime and test doctor rejection in implementation; database behavior untested. |
| Dependencies and frontend harness | BLOCKED | Validate Python/Node versions and pins, create locks and required harness in milestone 01. |
| Real model integration/release | BLOCKED | Acquire/verify raw data, prepare graph with provenance, install numerical dependencies, then implement/test real stack. |
| Learning benefit | NOT_RUN | Offline preregistered evaluation and qualifying evidence required before claims. |

Next: **01 — foundation**, only upon user request. Read AGENTS.md, PLAN, BOUNDARIES,
STATUS, this report, and the next requested prompt; recheck changes/environment.
Establish isolated runtime/dependency locks and fixture harness; account for the
failed upstream chain-test import without adding chain libraries to core runtime.
The missing real data must remain explicit and fail any requested real-model gate.
No remote push, public deployment/tunnel, paid resource, transaction, PR, or social
post is authorized by this audit.
