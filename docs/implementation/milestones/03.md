# Milestone 03 — real adapter and observation isolation

**automated_status: PASS · human_review: NOT_REQUIRED · complete**

Executed [prompt 03](../../../flytrap-kit/prompts/03-controller.md) through
[resume](../../../flytrap-kit/prompts/resume.md), on 2026-09-12 UTC
(2026-09-11 US/Central). Only milestone 03 was implemented. Learning evidence is
**NOT_RUN**; learning remains disabled.

## Prerequisites and preservation

Milestones 00–02 were complete with no pending human reviews. Every file hash
recorded in milestone 02's final source identity matched at entry. Its raw-data,
regression and browser evidence remained present. `make data-doctor` freshly
verified pinned raw files, graph checksums, anatomy and mapping metadata.
The unchanged preparation layer's independent raw directed-count scan remains
valid evidence from milestone 02. See
[prerequisite-check.json](../../../artifacts/milestones/03/prerequisite-check.json).

HEAD remains `8748e5bd30794d14afeb3441904221b52a002cac`; work is local and
uncommitted. Existing user edits, kit/resume instructions, LICENSE, NOTICE, raw
data and attribution were preserved. Upstream `flyeye.py` gains optional explicit
annotation injection and two telemetry fields; `flysim.py` adds exact total spike
counts. Simulation, retinal sampling and motor math otherwise remain upstream.

Two bounded agents supplied synthetic tests, the real smoke test and independent
source review. Root integrated their work, added parity/architecture checks,
ran validation and inspected results. No milestone 04 implementation began.

## Delivered behavior

[FlyController](../../../flytrap/controllers/fly.py) loads verified graph and
annotation files plus explicit parameter, calibration and checkpoint files. It
implements reset, step and a bounded serialized Observation-only envelope. Its
SHA256 seed stream depends only on run seed and step index. `windowed_reset`
is preserved. Outputs contain bounded upstream movement, genuine total spikes
over all neurons and separately identified neural milliseconds.

Baseline descriptors bind graph, annotation, config-file and model-source hashes.
All gains are explicitly one; no inherited mushroom state or calibrated upstream
gains load. Malformed inputs/configs, missing populations, unknown input fields,
nonfinite neural results and model failures raise errors. Failed resets/model
calls invalidate active runs. Mutable Observation instances are revalidated;
subclass/extra-field inputs fail. Fixture opt-in and labeling remain explicit.

[CONTROLLER](../CONTROLLER.md) specifies commands, paths, seed derivation, retinal
edge clipping, motor equations and limitations. `controller-baseline` creates a
new descriptor without overwriting an existing file; `controller-doctor` verifies
loading and reports provenance. The wheel includes upstream model modules, and
its separately installed adapter reproduced checkout output from an unrelated cwd.

Crop parity is with FlyEye on the clipped 16×16 raster. An arbitrary unpadded
full scene can differ at the u/v=1 endpoint. Milestone 04 must implement the
authoritative crop with these documented semantics.

## Measured real execution

Graph SHA256 is
`4279e2222475b986a096634ea5f6ac9518d0f8feff3f3c2ed52ad1139d3cd2ed`:
165,122 neurons and 10,228,000 fast edges. The adapter uses 1,767 L1 and 1,767 L2
cells, preserving upstream exclusion of 9 L1 and 12 L2 cells without retinal
coordinates. Required motors are present: DNa02 L/R 1/1, DNa01 L/R 1/1, MDN 4,
DNp09 2, MN9 2.

The final smoke used seed 20260911, all-one gains, 180 Hz L1 drive and 180×0.6 Hz
L2 drive. Each window ran 100×0.2 ms neural steps. Two observations were run
sequentially, then repeated after reset.

| Window | dx / dy | Exact spikes | Final wall seconds |
|---|---|---:|---:|
| Blank, index 0 | 0 / 0.2592592593 | 194,123 | 0.3932 |
| Checkerboard, index 1 | −0.1111111111 / 0.5185185185 | 186,720 | 0.3675 |
| Blank after reset | identical serialized output | 194,123 | 0.3784 |
| Checkerboard after reset | identical serialized output | 186,720 | 0.3584 |

Baseline preparation took 1.96 seconds; controller loading took 3.24 seconds.
These local measurements imply no real-time or capacity promise. The observations
use different step seeds: this is operation/reset evidence, **not a controlled
visual-sensitivity experiment** or challenge-feasibility result. No arena
trajectory or learning benefit was measured.

## Executed validation

[commands.jsonl](../../../artifacts/milestones/03/commands.jsonl) records exact
argv, exits, UTC timestamps, durations and stdout/stderr paths.
[test-counts.json](../../../artifacts/milestones/03/test-counts.json) derives counts
from inspected JUnit. Paths below are relative to `artifacts/milestones/03/`.

| Command / check | Actual result | Evidence |
|---|---|---|
| `make data-doctor` | Exit 0; raw and graph integrity PASS | `data-doctor.*.log` |
| `make verify test-upstream EVIDENCE=artifacts/milestones/03` | Exit 0; all ordinary required checks PASS | `verify.*.log` |
| Python within verify | 1,192 collected/passed, including 97 controller cases; 0 failures/errors/skips | `python.xml`, `coverage.*` |
| Upstream regressions | 150 collected/passed; 0 failures/errors/skips | `upstream.xml` |
| Web contracts / components | 89 / 9 collected/passed; 0 failures/skips | `web-contract.xml`, `ui.xml` |
| Desktop/mobile browser startup | 2 passed; 0 failed/skipped/flaky | `browser/report.json`, captures/traces |
| Types, generated contracts, lint, Python/web builds, dependencies | PASS; locks unchanged, npm audit zero vulnerabilities | `verify.*.log` |
| `make test-controller-real EVIDENCE=artifacts/milestones/03` final | Exit 0; 1 collected/passed, 0 failures/errors/skips; 4 actual neural windows | `real-controller-final.*.log`, `real-controller.xml` |
| Explicit `controller-baseline` / `controller-doctor` CLI commands | Exit 0 each; retained baseline and structured load provenance | `baseline-cli.*.log`, `controller-doctor.*.log`, `baseline.json` |
| Installed-wheel fixture execution, `wheel_probe.py` | Exit 0; installed imports and actual output parity | `wheel.*.log`, `wheel-install/`, `wheel-fixture/` |
| Privileged-import / dishonest-fixture mutations | Each selected test exited 1, with 1 intended failure | `privileged_import/`, `dishonest_fixture/`, `mutation-results.json` |
| `make test-controller-real` with absent raw/graph roots | Make exit 2; 1 collected, 1 setup error, 0 passed/skipped | `missing-controller.*.log`, `missing-model/real-controller.xml` |
| `make verify-real` | Make exit 2 / CLI exit 1; BLOCKED on later integration | `verify-real.*.log` |

Initial focused runs collected 57, then 96 passing controller tests. The final
ordinary suite contains 97, including partial-missing-retina regression. Initial
real setup failed because validation required coordinates for every L1/L2 cell.
Inspection established upstream's missing-coordinate mask; this is now preserved
and tested, with exclusions reported. Initial failure logs remain. The repaired
real smoke passed, then passed again after renaming its test module to prevent
duplicate names in combined collection. Diagnostic failures and repeated runs
are not added to final passing counts.

Root opened both browser captures: fixture labels, closed admission and
not-evaluated learning are legible, and mobile content fits. Trace inspection
confirmed HTTP 200 config responses; each development trace also contains an
aborted duplicate request. `browser/visual-review.json` records this startup-only
review. The existing AnyIO deprecation warning remains.

Python is 3.14.7, Node 26.8.2 and linked SQLite 3.53.4. Source/environment identity
is in `final-source-identity.json`; `summary.json` binds the handoff to evidence.
Hosted CI, full real-model system E2E and deployment remain unexecuted.

## Boundary handoff

| Boundary | Status | Remaining work |
|---|---|---|
| Raw data / graph prerequisite | PASS | Verified bundle retained; construction unchanged. |
| Graph → explicit real adapter | PASS | No remaining milestone 03 prerequisite. |
| Observation / serialized input / import isolation | PASS | Preserve pixels-only evaluation interface. |
| Retinal / motor parity and exact telemetry | PASS | 04 defines canonical arena/crop/physics. |
| Real task feasibility | NOT_RUN | 05 controlled behavior measurements. |
| Real worker / full browser release | BLOCKED | Later worker, API, replay and E2E milestones. |
| Learning evidence | NOT_RUN | Learning and improvement claims disabled. |

Next: **04 — arena, renderer and scoring**, on the next explicit invocation.
No human gate applies to 03. No commit, push, PR, external deployment, paid
service, token transaction or social post occurred.
