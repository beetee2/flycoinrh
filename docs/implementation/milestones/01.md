# Milestone 01 — executable foundation

**Engineering: PASS / complete. Real-model release: BLOCKED. Learning: NOT_RUN.**
Executed [prompt 01](../../../flytrap-kit/prompts/01-foundation.md), selected by
[resume](../../../flytrap-kit/prompts/resume.md). Milestone 00 had completed its audit
deliverables. Its baseline dependency failure was repaired here without changing
upstream tests. Milestone 02 has not started.

## Context and preservation

HEAD remains `8748e5bd30794d14afeb3441904221b52a002cac`. Initial status contained the
previous audit's `.gitignore`, AGENTS.md/docs, and supplied kit. Comparison to the
prior manifest found only the user's updated resume prompt; it was preserved in
the new initial manifest. Existing upstream programs, tests, HTML pages, data assets,
LICENSE and NOTICE remain intact. Changes are local and uncommitted. No push, PR,
deployment, transaction, paid service, or social post was made.

Bounded agents implemented contracts and frontend; parent integrated and verified
both. Read-only integration review identified ambiguous doctor path labels, now
explicitly named upstream checkout probes, separate from operator storage roots.

## Implemented behavior

Python 3.14.7 and Node 26.8.2/npm 12.0.2 are pinned through runtime files, pyproject,
uv.lock, and the web package lock. Upstream direct pins resolved and installed in
`.venv`. Core runtime contains FastAPI/Pydantic/uvicorn; numerical model extras and
upstream chain/browser dependencies are separate. Original requirements files are
intact. `make bootstrap` synchronizes locks and installs the pinned Chromium build.

`flytrap/contracts.py` defines eleven versioned root contracts and bounded nested
models. Unknown fields, nonfinite values, invalid IDs/seeds/enums and excessive
arrays are rejected. Shared fixtures establish Python/JSON Schema/AJV parity;
integer-valued JSON numbers such as 1.0 are accepted consistently without coercing
strings or booleans. Challenge hashes are constructed server-side and an explicit
verifier rejects tampering. Structural schemas do not prove registry approval,
allowed state transitions, replay integrity or scientific claims.

Generators produce checked JSON Schema, TypeScript and OpenAPI artifacts. The
[contract agreement](../contracts/README.md) records schema evolution, endpoint/state
proposals, seed policy and ownership. Version 1 uses a fixed normalized 16×16 crop;
real-eye sampling must prove parity or explicitly version a later shape change.

`flytrap/api/` implements health/config routes and lifespan. The SQLite connection
policy requires linked SQLite ≥3.51.3, file-backed WAL, foreign keys, and bounded busy
timeout. Fixture startup creates operator-selected roots and starts a separate
process from `flytrap/worker/`. Bounded IPC accepts only Observation and returns a
validated ControllerOutput. The explicit synthetic controller reports zero neural
telemetry. A tiny invented graph separately exercises actual upstream FlyBrain
loading, propagation and repeated window resets, with synthetic labeling.

This is a startup harness, not the durable queue/coordinator. It admits no runs.
Real mode is the default and returns readiness 503; production configuration rejects
fixtures. Errors never switch real mode to fixtures. Local serving binds loopback.
No upstream wallet, roaming, environment-loader or narrator module is imported.

The React page shows `FIXTURE — NOT REAL MODEL`, actual API capabilities and
loading/error/retry states. It has no neural animation. Existing web/live.html and
web/roam.html survive. The Makefile exposes plan targets; ordinary verification
covers current foundation checks, while real/release targets fail on missing
prerequisites. Hosted ephemeral CI is configured to run foundation plus upstream
tests. The hosted workflow itself has **not executed**.

## Actual commands and results

Commands ran from `/home/kernel_sanders/dev/flycoinrh`. Exact argv, UTC timestamps,
durations, exits and logs are in
[`commands.jsonl`](../../../artifacts/milestones/01/commands.jsonl) and
[`contract-commands.jsonl`](../../../artifacts/milestones/01/contract-commands.jsonl).
Evidence below is relative to `artifacts/milestones/01/`.

| Executed command | Exit / result | Evidence |
|---|---|---|
| `uv pip compile requirements.txt --python-version 3.14 --output-file artifacts/milestones/01/upstream-resolved.txt` | 0; 50 upstream packages resolved | `upstream-pin-resolution.*.log`, `upstream-resolved.txt` |
| `uv lock`; `uv sync --locked --extra model --group upstream` | Both 0; 71 resolved entries, 69 installed distributions | `uv-lock.*.log`, `uv-sync.*.log` |
| `.venv/bin/python -m pytest test_calibration.py test_olfaction.py test_pons.py test_voice.py test_xpost.py -q --junitxml=artifacts/milestones/01/upstream.xml` | 0; 150 passed, 0 failed/skipped | `upstream-baseline.*.log`, `upstream.xml` |
| `make generate` | 0; schemas/OpenAPI/types generated | `generate.*.log` |
| `make verify` | 0; all ordinary foundation checks passed | `verify.*.log` |
| `make bootstrap` | 0; locked install and Chromium available | `bootstrap.*.log` |
| `make verify-real` | 2; CLI exit 1, BLOCKED | `verify-real.*.log` |
| `make test-e2e-real` | 2; BLOCKED; no real-model tests collected | `test-e2e-real.*.log` |
| `make verify-release` | 2; BLOCKED; no release pass | `verify-release.*.log` |

Final `make verify` expanded to doctor, schema/OpenAPI/type drift checks, Ruff,
pytest with branch coverage, web contracts, components, actual local Playwright
services, Python sdist/wheel build, TypeScript/Vite build, `uv lock --check`,
`uv pip check`, and `npm audit --audit-level=high`.

| Suite | Passed | Failed | Skipped | Evidence |
|---|---:|---:|---:|---|
| Python unit/property/contracts/file DB/API/process | 1,039 | 0 | 0 | `python.xml`, `coverage.json`, `coverage.xml` |
| Web runtime contract parity / nonfinite numbers | 89 | 0 | 0 | `web-contract.xml` |
| React component / accessibility cases | 9 | 0 | 0 | `ui.xml` |
| Desktop/mobile browser startup | 2 | 0 | 0 | `browser/report.json`, screenshots and traces |
| Upstream baseline, separately executed | 150 | 0 | 0 | `upstream.xml` |

Python includes parameterized numeric mutation probes, not independent biological
experiments. Collection guard tests run disposable empty/all-skipped suites and
require nonzero exits. Coverage is 266/339 statements and 55/84 branches; uncovered
paths are retained. Child-process/browser-server code is not fully attributed by
parent-process coverage. No coverage threshold substitutes for boundary tests.

Bootstrap after verification reinstalled the same locks; source and dependencies
remained equivalent, so passing evidence was retained. Installed dependencies are
compatible and npm reported 0 known vulnerabilities. No Python vulnerability scan,
production load test, or security audit is claimed. Starlette emitted one AnyIO
deprecation warning, retained without suppression. Playwright used its Ubuntu
fallback build on Arch; both actual browser tests passed.

## Failures retained and visual review

Initial frontend build failed on an unnecessary CSS import; the first browser start
used an unsupported CLI argument. Both were corrected and retested. Contract tests
found incorrectly exported integer-bound keywords after normalization (5 failures);
field-level normalization fixed the actual schema, followed by 1,018 passing
contract/property tests and final integrated verification. Failure logs remain in
`frontend-build.*`, `frontend-e2e.*`, and `contract-check-0.log`. Acceptance criteria
were not weakened.

Parent opened both final `browser/results/*/foundation.png` images: fixture label is
prominent; unavailable model, closed admission and not-evaluated learning are
readable; mobile wraps without clipping/overflow; keyboard focus is visible.
Trace network records contain actual config/health responses via the local Vite
proxy. No API interception is used. `browser/visual-review.json` records scope.
This is startup evidence, not a challenge run, full accessibility audit, replay or
real model trajectory.

## Handoff

| Boundary | Status | Remaining work |
|---|---|---|
| Foundation and upstream prerequisite | PASS | Preserve locked runtimes, fixture labels, generated contracts and checks. |
| Real data/model/release | BLOCKED | Raw Feather files/graph absent; acquire verified data, retain anatomy before fast signs, implement real adapter in its milestone. |
| Durable queue/replay/full API/UI/security/containers | NOT_RUN | Later milestone acceptance remains required. |
| Hosted CI / deployment | NOT_RUN | Workflow configured only; no remote action. |
| Learning benefit | NOT_RUN | Claims/comparisons disabled pending qualifying experiments. |

Next: **02 — connectome data and anatomical metadata**, on the next explicit resume.
Recheck source/config hashes in `final-source-identity.json` and `summary.json`.
Retain ignored evidence when moving workspaces. Milestone 02 was not read or started.

Implementation references consulted: [uv locked sync](https://docs.astral.sh/uv/concepts/projects/sync/)
and [FastAPI lifespan](https://fastapi.tiangolo.com/advanced/events/). Executed local
checks establish the reported outcomes.
# CI runtime correction — 2026-09-12

Run [34667124950](https://github.com/beetee2/flycoinrh/actions/runs/34667124950)
failed on source `a1ecbdc44d9397c97a4af41f3f4abf84c1d050e0`: the
setup-python interpreter linked SQLite 3.45.1, rejected by the existing 3.51.3
minimum. Foundation CI now requires uv-managed Python through job-level
`UV_MANAGED_PYTHON=1` and removes setup-python. uv is pinned to 0.12.13 because
0.10.9 has no managed Python 3.14.7 download. The managed interpreter links
SQLite 3.53.1. Python and dependency pins and the SQLite guard are unchanged.

**Local validation: PASS.** In a clean worktree, `make bootstrap` and
`make verify test-upstream` both exited 0 with uv 0.12.13 and
`UV_MANAGED_PYTHON=1`: 1,406 Python, 150 upstream, 89 web contract, 9 component,
and 2 browser tests passed; zero failures/skips. Generated-file checks, lint,
builds, lock consistency, dependency checks and npm audit passed. The desktop
capture shows the fixture label, unavailable real model and closed admission.
Commands, source identity, counts and logs are under ignored
`artifacts/milestones/01/ci-sqlite-fix/`; JUnit and browser evidence are in its
parent directory. Hosted validation is pending the branch push. This correction
does not advance the milestone 05 human review gate.
